# Signup popup

The signup UI lives in `src/components/SignupPopup/SignupPopup.jsx`.

**Deployment is Cloudflare-only.** Follow the step-by-step guide:

→ **[docs/CLOUDFLARE_DEPLOY.md](./CLOUDFLARE_DEPLOY.md)**

That guide covers Turnstile, Resend (or Gmail fallback), Pages build settings, environment variables, custom domain, and local `wrangler pages dev`.
